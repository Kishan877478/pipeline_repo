pipeline {
agent any
stages {
  stage('Build') {
    stages {
      // One or more stages need to be included within the stages block.
    }
  }

  stage('Deploy') {
    stages {
      // One or more stages need to be included within the stages block.
    }
  }

  stage('Test') {
    stages {
      // One or more stages need to be included within the stages block.
    }
  }

}
